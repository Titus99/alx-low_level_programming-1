#include "main.h"

/**
 * print_last_digit - Entry point
 * @m:int to print
 * Return: 0
*/

int print_last_digit(int m)
{
	int last_digit;

	if (n > 0)

		m = -m;

	last_digit = m % 10;

	if  (last_digit < 0)

		last_digit = -last_digit;

	_putchar(last_digit + '0');

	return (a);
}
