#include "main.h"
#include <stdio.h>

/**
 * _abs - Entry point
 * @m: compute value of integer
 * Return: absolute value of int
*/

int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	return (-m);
}
